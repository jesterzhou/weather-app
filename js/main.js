/**
 * rendering via three.js 
 * 
 * using threejs, render a blender gltf model which would display weather forecast.
 * @author Jester Zhou
 */

import * as THREE from 'three';

const scene = new THREE.Scene()